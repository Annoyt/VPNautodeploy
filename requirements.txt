# VPN Bot Requirements

# Core
aiogram>=3.4.0
aiosqlite>=0.20.0
python-dotenv>=1.0.0
psutil>=5.9.0
aiohttp>=3.9.0
requests>=2.28.0
apscheduler>=3.10.0

# GeoIP — offline IP→country lookup. The DB file (~3 MB, db-ip lite)
# is downloaded on first use and refreshed weekly; see
# bot/services/geoip.py.
maxminddb>=2.5.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Development
black>=23.0.0
flake8>=6.0.0
